@model IEnumerable<SistemaNotas1.Models.TbRol>

@{
    ViewData["Title"] = "Roles";
}

<br />

<br />
<br />
<br />
<br />
<br />
<br />
<div class="container">
    <h1>Roles</h1>

    <p>
        <a class="btn btn-success" asp-action="Create">Crear Roles</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Rol)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Rol)
                    </td>
                    <td>
                        <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.CodRol">Editar</a> |
                        <a class="btn btn-primary" asp-action="Details" asp-route-id="@item.CodRol">Detalles</a> |
                        <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.CodRol">Eliminar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>