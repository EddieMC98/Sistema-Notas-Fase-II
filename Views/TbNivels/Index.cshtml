@model IEnumerable<SistemaNotas1.Models.TbNivel>

@{
    ViewData["Title"] = "Niveles";
}
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<div class="container">
    <h1>Niveles</h1>

    <p>
        <a class="btn btn-success" asp-action="Create">Crear Nivel</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Nombre)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nombre)
                    </td>
                    <td>
                        <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.CodNivel">Editar</a> |
                        <a class="btn btn-primary" asp-action="Details" asp-route-id="@item.CodNivel">Detalles</a> |
                        <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.CodNivel">Eliminar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>