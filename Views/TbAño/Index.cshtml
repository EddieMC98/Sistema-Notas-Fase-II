@model IEnumerable<SistemaNotas1.Models.TbAño>

@{
    ViewData["Title"] = "Lista Año";
}
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<div class="container">
    <h1>Lista Año</h1>

    <p>
        <a class="btn btn-success" asp-action="Create">Crear Año</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Numero)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Numero)
                    </td>
                    <td>
                        <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.CodAño">Editar</a> |
                        <a class="btn btn-primary" asp-action="Details" asp-route-id="@item.CodAño">Detalles</a> |
                        <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.CodAño">Elminar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>