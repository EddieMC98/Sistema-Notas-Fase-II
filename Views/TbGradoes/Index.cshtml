@model IEnumerable<SistemaNotas1.Models.TbGrado>

@{
    ViewData["Title"] = "Index";
}

<br />
<br />
<br />
<br />
<br />
<br />
<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CodGradoSiguiente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SiguienteNivel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodNivel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CodGradoSiguiente)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SiguienteNivel)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CodNivel)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.CodGrado">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.CodGrado">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.CodGrado">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
