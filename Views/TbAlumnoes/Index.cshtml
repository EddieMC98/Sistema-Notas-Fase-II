@model IEnumerable<SistemaNotas1.Models.TbAlumno>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Codigo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodInformacionPersonal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodUsuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodGrado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Codigo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodInformacionPersonal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodUsuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodGrado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CodAlumno">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CodAlumno">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CodAlumno">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
